# FabOps Config Assets

- `capability_metadata.schema.yaml` – validation schema for each capability's `metadata.yaml`.
- `fabops.config.template.json` – starter config for new projects.

Use this directory to store scripts and schemas consumed by automation.
